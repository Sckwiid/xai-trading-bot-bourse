{{MARKET_NEWS}}

MARKET CONTEXT
Current time: {{NOW_ET}}

TASK
- Analyze the provided news and live account snapshot.
- Produce exactly one decision block per ticker in the watchlist.
- Keep the watchlist order.

DECISION GUIDELINES
- If a ticker has no clear edge from the provided data, use `hold`.
- For `hold`, set numeric defaults consistently (for example quantity 0, leverage 1, risk 0).
- If `hold`, set `HOLD_REASON` with a short explicit cause (example: `no_clear_edge`, `nav_zero`, `missing_price_data`, `risk_too_high`).
- For `buy_to_enter`, provide a concrete invalidation condition plus non-zero quantity.
- For `sell`, use it only if a thesis is clearly broken or risk regime has deteriorated.

FORMAT QUALITY RULES
- Every numeric field must contain a number.
- Do not leave fields empty.
- Do not output `None`, `null`, or placeholders.
- Use plain tickers (no brackets).
- `COIN` must use `xyz:TICKER`.

WATCHLIST TO PROCESS
{{WATCHLIST}}

{{LIVE_ACCOUNT_STATE}}
